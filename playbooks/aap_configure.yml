---
# Config as code: creates or updates every AAP object for the use cases from
# the files in aap_config/.
#
# Requirements: an execution environment containing infra.aap_configuration,
# ansible.platform, ansible.controller and ansible.eda (see execution-environment/),
# plus a "Red Hat Ansible Automation Platform" credential on the job template.
# From a workstation, export AAP_HOSTNAME and AAP_TOKEN (or AAP_USERNAME/AAP_PASSWORD).
- name: Configure Ansible Automation Platform for the use cases
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    aap_hostname: >-
      {{ (lookup('ansible.builtin.env', 'AAP_HOSTNAME') or lookup('ansible.builtin.env', 'CONTROLLER_HOST'))
         | regex_replace('/+$', '') | regex_replace('/api/controller$', '') }}
    aap_username: "{{ lookup('ansible.builtin.env', 'AAP_USERNAME') or lookup('ansible.builtin.env', 'CONTROLLER_USERNAME') }}"
    aap_password: "{{ lookup('ansible.builtin.env', 'AAP_PASSWORD') or lookup('ansible.builtin.env', 'CONTROLLER_PASSWORD') }}"
    aap_token: "{{ lookup('ansible.builtin.env', 'AAP_TOKEN') or lookup('ansible.builtin.env', 'CONTROLLER_OAUTH_TOKEN') }}"
    aap_validate_certs: >-
      {{ (lookup('ansible.builtin.env', 'AAP_VALIDATE_CERTS') or lookup('ansible.builtin.env', 'CONTROLLER_VERIFY_SSL') or 'true') | bool }}
    eda_project_import_retries: 30

  pre_tasks:
    - name: Load the AAP object definitions
      ansible.builtin.include_vars:
        dir: "{{ playbook_dir }}/../aap_config"
        extensions:
          - yml

    - name: Check required settings
      ansible.builtin.assert:
        that:
          - aap_hostname | length > 8
          - aap_token | length > 0 or aap_username | length > 0
          - uc_scm_url | length > 0
        fail_msg: >-
          Missing connection settings. Attach a "Red Hat Ansible Automation Platform" credential
          or export AAP_HOSTNAME with AAP_TOKEN (or AAP_USERNAME/AAP_PASSWORD).
        quiet: true

    # The collection roles prefer a token when one is defined, even if empty.
    - name: Drop empty authentication settings
      ansible.builtin.set_fact:
        uc_api_base: "{{ aap_hostname if aap_hostname is match('https?://') else 'https://' ~ aap_hostname }}"
        uc_use_token: "{{ aap_token | length > 0 }}"
        uc_api_token: "{{ aap_token }}"
        uc_api_username: "{{ aap_username }}"
        uc_api_password: "{{ aap_password }}"
        aap_token: "{{ omit if aap_token | length == 0 else aap_token }}"
        aap_username: "{{ omit if aap_username | length == 0 else aap_username }}"
        aap_password: "{{ omit if aap_password | length == 0 else aap_password }}"

  tasks:
    - name: Apply controller objects, EDA credentials, project and event streams
      ansible.builtin.include_role:
        name: infra.aap_configuration.dispatch

    # Pull the latest rulebooks, then wait: an activation can only be created
    # once the EDA project has finished importing.
    - name: Find the EDA project
      ansible.builtin.uri:
        url: "{{ uc_api_base }}/api/eda/v1/projects/?name={{ 'AAP Use Cases' | urlencode }}"
        headers: "{{ uc_api_headers }}"
        url_username: "{{ omit if uc_use_token else uc_api_username }}"
        url_password: "{{ omit if uc_use_token else uc_api_password }}"
        force_basic_auth: "{{ not uc_use_token }}"
        validate_certs: "{{ aap_validate_certs }}"
      register: eda_project_lookup
      vars:
        uc_api_headers: "{{ {'Authorization': 'Bearer ' ~ uc_api_token} if uc_use_token else {} }}"

    - name: Sync the EDA project (a sync already in progress is fine)
      ansible.builtin.uri:
        url: "{{ uc_api_base }}/api/eda/v1/projects/{{ eda_project_lookup.json.results[0].id }}/sync/"
        method: POST
        headers: "{{ uc_api_headers }}"
        url_username: "{{ omit if uc_use_token else uc_api_username }}"
        url_password: "{{ omit if uc_use_token else uc_api_password }}"
        force_basic_auth: "{{ not uc_use_token }}"
        validate_certs: "{{ aap_validate_certs }}"
        status_code: [200, 201, 202, 409]
      vars:
        uc_api_headers: "{{ {'Authorization': 'Bearer ' ~ uc_api_token} if uc_use_token else {} }}"

    - name: Wait for the EDA project import to finish
      ansible.builtin.uri:
        url: "{{ uc_api_base }}/api/eda/v1/projects/{{ eda_project_lookup.json.results[0].id }}/"
        headers: "{{ uc_api_headers }}"
        url_username: "{{ omit if uc_use_token else uc_api_username }}"
        url_password: "{{ omit if uc_use_token else uc_api_password }}"
        force_basic_auth: "{{ not uc_use_token }}"
        validate_certs: "{{ aap_validate_certs }}"
      register: eda_project
      # Wait for the sync requested above, not the previous import.
      until:
        - eda_project.json.modified_at | default('') != eda_project_lookup.json.results[0].modified_at | default('')
        - eda_project.json.import_state | default('') in ['completed', 'failed']
      retries: "{{ eda_project_import_retries }}"
      delay: 10
      vars:
        uc_api_headers: "{{ {'Authorization': 'Bearer ' ~ uc_api_token} if uc_use_token else {} }}"

    - name: Stop if the EDA project import failed
      ansible.builtin.assert:
        that: eda_project.json.import_state == 'completed'
        fail_msg: "EDA project import failed: {{ eda_project.json.import_error | default('see the EDA project page') }}"
        quiet: true

    # EDA refuses to update a running activation, and an activation keeps the
    # rulebook it was created with. So existing activations are left alone
    # unless uc_recreate_activations=true, which deletes and recreates them
    # (use it after changing a rulebook).
    - name: Look up existing rulebook activations
      ansible.builtin.uri:
        url: "{{ uc_api_base }}/api/eda/v1/activations/?name={{ item.name | urlencode }}"
        headers: "{{ uc_api_headers }}"
        url_username: "{{ omit if uc_use_token else uc_api_username }}"
        url_password: "{{ omit if uc_use_token else uc_api_password }}"
        force_basic_auth: "{{ not uc_use_token }}"
        validate_certs: "{{ aap_validate_certs }}"
      loop: "{{ uc_eda_rulebook_activations }}"
      loop_control:
        label: "{{ item.name }}"
      register: uc_activation_lookup
      vars:
        uc_api_headers: "{{ {'Authorization': 'Bearer ' ~ uc_api_token} if uc_use_token else {} }}"

    - name: Delete activations that should be recreated
      ansible.builtin.uri:
        url: "{{ uc_api_base }}/api/eda/v1/activations/{{ item.json.results[0].id }}/"
        method: DELETE
        headers: "{{ uc_api_headers }}"
        url_username: "{{ omit if uc_use_token else uc_api_username }}"
        url_password: "{{ omit if uc_use_token else uc_api_password }}"
        force_basic_auth: "{{ not uc_use_token }}"
        validate_certs: "{{ aap_validate_certs }}"
        status_code: [202, 204]
      loop: "{{ uc_activation_lookup.results }}"
      loop_control:
        label: "{{ item.item.name }}"
      when:
        - uc_recreate_activations | default(false) | bool
        - item.json.results | length > 0
      vars:
        uc_api_headers: "{{ {'Authorization': 'Bearer ' ~ uc_api_token} if uc_use_token else {} }}"

    - name: Wait for deleted activations to disappear
      ansible.builtin.uri:
        url: "{{ uc_api_base }}/api/eda/v1/activations/?name={{ item.item.name | urlencode }}"
        headers: "{{ uc_api_headers }}"
        url_username: "{{ omit if uc_use_token else uc_api_username }}"
        url_password: "{{ omit if uc_use_token else uc_api_password }}"
        force_basic_auth: "{{ not uc_use_token }}"
        validate_certs: "{{ aap_validate_certs }}"
      loop: "{{ uc_activation_lookup.results }}"
      loop_control:
        label: "{{ item.item.name }}"
      register: uc_activation_gone
      until: uc_activation_gone.json.results | length == 0
      retries: 30
      delay: 5
      when:
        - uc_recreate_activations | default(false) | bool
        - item.json.results | length > 0
      vars:
        uc_api_headers: "{{ {'Authorization': 'Bearer ' ~ uc_api_token} if uc_use_token else {} }}"

    - name: Decide which activations to create
      ansible.builtin.set_fact:
        uc_activations_to_apply: >-
          {{ uc_eda_rulebook_activations
             if uc_recreate_activations | default(false) | bool
             else (uc_activation_lookup.results | rejectattr('json.results') | map(attribute='item') | list) }}

    - name: Apply rulebook activations that do not exist yet
      ansible.builtin.include_role:
        name: infra.aap_configuration.eda_rulebook_activations
      vars:
        eda_rulebook_activations: "{{ uc_activations_to_apply }}"
      when: uc_activations_to_apply | length > 0

    - name: Report activations left unchanged
      ansible.builtin.debug:
        msg: >-
          Left running (unchanged): {{ uc_activation_lookup.results | selectattr('json.results') | map(attribute='item.name') | join(', ') }}.
          Re-run with uc_recreate_activations=true after changing a rulebook.
      when:
        - not (uc_recreate_activations | default(false) | bool)
        - uc_activation_lookup.results | selectattr('json.results') | list | length > 0
